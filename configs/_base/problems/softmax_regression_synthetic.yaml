# Multinomial logistic regression on synthetic Gaussian features.
# Default K=3 keeps the parameter count tractable (n * K = 60).
problem:
  type: softmax_regression_synthetic
  params:
    m: 500
    n: 20
    K: 3
    reg: 1.0e-3
    seed: 0
