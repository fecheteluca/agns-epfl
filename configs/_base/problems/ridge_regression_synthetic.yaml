# Ridge regression on synthetic Gaussian features.  Constant Hessian
# (X^T X / m + reg I) -> the cleanest test bed for accelerated linear
# convergence (the convergence theory).
problem:
  type: ridge_regression_synthetic
  params:
    m: 1000
    n: 100
    reg: 1.0e-2
    seed: 0
