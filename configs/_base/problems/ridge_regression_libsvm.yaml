# Ridge regression on a LIBSVM regression dataset.  ``path`` defaults
# to ``data/regression/cadata`` (California housing, ~20k samples,
# 8 features).
problem:
  type: ridge_regression_libsvm
  params:
    path: data/regression/cadata
    reg: 1.0e-2
    max_samples: null
    seed: 0
