# Runtime + development dependencies for the `agns` package.
#
# Pinned ranges are kept loose so the package works under recent toolchains;
# for a fully reproducible environment, install with `pip install -e .[dev]`
# which uses the pyproject.toml constraints.

# Runtime
numpy>=1.26,<3
scipy>=1.13,<2
scikit-learn>=1.4,<2
pandas>=2.2,<3
pyyaml>=6,<7
matplotlib>=3.8,<4
torch>=2.0

# Tests + property-based fuzzing
pytest>=8,<9
pytest-cov>=4,<6
pytest-xdist>=3,<4
hypothesis>=6,<7

# Lint + format + types
ruff>=0.6,<1
mypy>=1.10,<2
types-pyyaml
