# Core numerics
numpy>=1.24.0
scipy>=1.10.0

# Data loaders for the LIBSVM (a9a, mushrooms, w8a) experiments
scikit-learn>=1.2.0

# Configuration files for experiments
pyyaml>=6.0.0

# Plotting (main.py uses Agg backend; matplotlib is the only renderer used)
matplotlib>=3.7.0

# Notebook runtime for notebooks/examples.ipynb (optional; uncomment if needed)
# jupyterlab>=4.0.0
# ipython>=8.10.0

# Test runner (optional; tests also run with `python tests/test_*.py`)
# pytest>=7.0.0
